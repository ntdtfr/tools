# gravitee/k8s/base/api-gateway/service.yaml
---
apiVersion: v1
kind: Service
metadata:
  name: gateway
  labels:
    flexwiz.io/app: gateway
spec:
  selector:
    flexwiz.io/app: gateway
  type: ClusterIP
  ports:
  - port: 8082
    targetPort: 8082
    name: http
